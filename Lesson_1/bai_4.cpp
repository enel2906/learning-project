#include<stdio.h>
#include<string.h>

int main)
{
      char s[100];
      char a, b;
      int i = 0;
      scanf"%s %c %c",&s,&a,&b);

      
      forint i = 0;i<strlens);i++){
            ifs[i]==as[i] = b;
      }
      printf"%s",s);

}